Расставить элементы таким образом, как было показано в виде.

Код пишется в файле flow.css, файл styles.css трогать не нужно.

Media-запросы разрешены, но только на одной выбранной ширине, например, 990px. Дальнейшие перестроения могут быть сделаны и без media-запросов.